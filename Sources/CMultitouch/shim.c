// SwiftPM requires at least one compiled source file in a C target.
// The actual symbols live in the private MultitouchSupport framework,
// which is linked via the package's linker flags.
#include "CMultitouch.h"
